# TODO

- Add a central-driven SMP DFU demo flow to `samples/bluetooth/sap_demo` so the central can update a peripheral after SAP authentication. The demo payload should make the result obvious on-device, for example by changing the peripheral LED blink pattern after the DFU completes.
